#include <stdio.h>
#include <string.h>
 
int main () {

    char x[] = "Hello";
    
    // declare string y with "World"
    
    // concatenate the two strings
    
    // print the final string
    char y[] = "World";
  strcat(x, y);
    printf("%s", x);

}
